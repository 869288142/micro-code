const a = [1, 2, 3, 4, 5];

// Array.prototype.multiply = function () {
//   Array.prototype.push.apply(this, this.map(e => e**2))
// }
// Implement this
a.multiply()
console.log(a); // [1, 2, 3, 4, 5, 1, 4, 9, 16, 25]
